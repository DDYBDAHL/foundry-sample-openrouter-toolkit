{
  "type": "object",
  "properties": {
    "model_output_markdown": {
      "type": "string",
      "description": "The LLM response in Markdown format with model information"
    },
    "model_output_html": {
      "type": "string",
      "description": "The LLM response converted to HTML with model information"
    },
    "model_output_text": {
      "type": "string",
      "description": "The LLM response converted to text with model information"
    },
    "model": {
      "type": "string",
      "description": "The model used for the completion"
    },
    "tokens": {
      "type": "integer",
      "description": "Total number of tokens used"
    },
    "request_id": {
      "type": "string",
      "description": "Unique identifier for this request"
    },
    "execution_time_ms": {
      "type": "integer",
      "description": "Total execution time in milliseconds"
    }
  },
  "required": ["model_output_text", "model", "request_id"]
}
